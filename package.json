{
  "name": "ecommerce_ui",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf reports dist",
    "test:bdd": "npm run clean && cucumber-js",
    "test:safari": "npm run clean && BROWSER=safari cucumber-js",
    "test:trainStatus": "cucumber-js --config cucumber.json --tags @trainStatus",
    "test:trainStatus:safari": "BROWSER=safari cucumber-js --config cucumber.json --tags @trainStatus",
    "test:regression": "cucumber-js --config cucumber.json --tags @regression",
    "test:regression:safari": "BROWSER=safari cucumber-js --config cucumber.json --tags @regression",
    "report:cucumber": "ts-node src/utilities/generate-report.ts",
    "test:all": "npm run test:bdd && npm run report:cucumber",
    "lint": "tsc --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "cucumber-html-reporter": "^7.2.0",
    "rimraf": "^6.1.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
